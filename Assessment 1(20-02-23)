#include<stdio.h>
void main()
{
int n,ele,i=0,l;
printf("enter how many elements want to compare:");
scanf("%d",&n);

while(i<=n-1)
{
printf("enter the element:");
scanf("%d",&ele);
if(i==0)
l=ele;
if(ele<l)
l=ele;
i++;
}
printf("\nthe lowest number:%d",l);
}
